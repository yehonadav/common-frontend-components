import {DivType} from "common-frontend-components";
import {useAppbarStyles} from "../useAppbarStyles";

export const FlexSpace: DivType = props => <div className={useAppbarStyles().FlexSpace} {...props}/>;